extends layout

block content
  h1= title
  - const cartoon = ['cartoon_type','size','age']

  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
    div(class='row')
      each result, index in results
        div(class='col-sm-2' onclick='buy('+index+')') 'Cartoon Type:'  #{result.cartoon_type}  'Size:' #{result.size} 'Age:' #{result.age}